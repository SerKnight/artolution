$("#current-case-study").html("<%= j render(:partial => 'current_project', :locals => { project: @current_project } ) %>");
$("#current-case-study").slideDown()
history.pushState(null, "<%= @current_project.title %>", '/' + window.location.pathname.split('/').slice(1,3).join('/') + "/<%= @current_project.slug %>"   );
var height = $('.hero-container').height() + 
$('#service-show-container').height() + 
$('#all-studies').height() +
$('.capabilities-container').height();
TweenLite.to(window, 1.2, {scrollTo:{y: height, x:0}, ease:Power2.easeOut});